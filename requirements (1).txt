# ============================================================
#   🔥 POWERFUL & EXTRA SYSTEM REQUIREMENTS
#   Bot Host: Render Free Plan | Telegram Bot (pyTelegramBotAPI)
# ============================================================

# ── CORE TELEGRAM BOT ───────────────────────────────────────
pyTelegramBotAPI>=4.18.0          # Main bot framework (telebot)
requests>=2.31.0                   # HTTP requests + polling error handling

# ── WEB SERVER (Keep-Alive / Render Health Check) ───────────
flask>=3.0.0                       # Lightweight web server for /health endpoint
gunicorn>=21.2.0                   # Production WSGI server (use instead of flask dev server on Render)

# ── DATABASE ────────────────────────────────────────────────
# sqlite3 is built-in to Python — no install needed

# ── PROCESS & SYSTEM MONITORING ─────────────────────────────
psutil>=5.9.8                      # Process management, kill trees, CPU/RAM stats

# ── ASYNC & PERFORMANCE BOOST ───────────────────────────────
aiohttp>=3.9.3                     # Async HTTP (faster than requests for heavy load)
uvloop>=0.19.0                     # Ultra-fast async event loop (Linux only — perfect for Render)
httpx>=0.27.0                      # Modern HTTP client (sync + async)

# ── FILE HANDLING ────────────────────────────────────────────
python-magic>=0.4.27               # Detect real file types (MIME)
Pillow>=10.3.0                     # Image processing / thumbnails
PyPDF2>=3.0.1                      # PDF read/write support
python-docx>=1.1.2                 # DOCX file support
openpyxl>=3.1.2                    # Excel file support
zipfile36>=0.1.3                   # Enhanced zip handling (Python 3.6+ compat)

# ── SECURITY & ENCRYPTION ───────────────────────────────────
cryptography>=42.0.5               # Encrypt sensitive data / tokens
bcrypt>=4.1.2                      # Password hashing
python-dotenv>=1.0.1               # Load .env secrets safely

# ── SCHEDULING & TASKS ──────────────────────────────────────
APScheduler>=3.10.4                # Advanced task scheduler (cron jobs, intervals)
celery>=5.3.6                      # Distributed task queue (optional, heavy workloads)
redis>=5.0.3                       # In-memory cache / Celery broker (pair with Redis on Render)

# ── LOGGING & MONITORING ────────────────────────────────────
loguru>=0.7.2                      # Powerful logging with colors, rotation, compression
sentry-sdk>=2.3.1                  # Real-time error tracking (free tier available)
prometheus-client>=0.20.0          # Metrics endpoint for uptime monitoring

# ── NETWORK & ANTI-BAN TOOLS ────────────────────────────────
urllib3>=2.2.1                     # Low-level HTTP (used by requests)
certifi>=2024.2.2                  # Updated SSL certificates
tenacity>=8.2.3                    # Retry logic with exponential backoff (anti-flood)

# ── DATA & UTILITIES ────────────────────────────────────────
python-dateutil>=2.9.0             # Powerful date/time parsing
pytz>=2024.1                       # Timezone support
humanize>=4.9.0                    # Human-readable sizes, times, numbers
ujson>=5.9.0                       # Ultra-fast JSON (drop-in replacement for json)
pydantic>=2.7.0                    # Data validation / settings management

# ── OPTIONAL: AI / SMART FEATURES ───────────────────────────
# openai>=1.30.0                   # OpenAI API (uncomment if adding AI features)
# anthropic>=0.28.0                # Claude API (uncomment if adding Claude AI)
# google-generativeai>=0.5.4       # Gemini API (uncomment if needed)

# ── OPTIONAL: DATABASE UPGRADES ─────────────────────────────
# SQLAlchemy>=2.0.30               # Full ORM (upgrade from raw sqlite3)
# alembic>=1.13.1                  # DB migrations (use with SQLAlchemy)
# psycopg2-binary>=2.9.9           # PostgreSQL driver (use Render PostgreSQL free tier)

# ── OPTIONAL: NODE.JS BOT SUPPORT ───────────────────────────
# (install via package.json, not pip)
# node-telegram-bot-api            # If running JS bots
# axios                            # HTTP for Node bots
# dotenv                           # .env support for Node

# ============================================================
#   📌 RENDER FREE PLAN NOTES
# ============================================================
# 1. Add a render.yaml or Procfile:
#       web: gunicorn aadihost:app --bind 0.0.0.0:$PORT
# 2. Set ENV VARS in Render dashboard (never hardcode tokens!)
#       TOKEN=your_bot_token
#       OWNER_ID=your_id
# 3. Use UptimeRobot (free) to ping your Render URL every 5 min
#       → prevents sleep better than self-pinging
# 4. Redis (free tier on Render) → use for session/cache instead of in-memory dicts
# 5. PostgreSQL (free tier on Render) → upgrade from SQLite for persistence
# ============================================================
